import React from "react";
import { ComponentMeta, ComponentStory } from "@storybook/react";
import BadgeAvatars from ".";

export default {
  title: "BadgeAvatars",
  component: BadgeAvatars,
} as ComponentMeta<typeof BadgeAvatars>;

// const Template: ComponentStory<typeof BadgeAvatars> = args => <BadgeAvatars {...args} />

// const Template: ComponentStory<typeof Header> = (args) => <Header {...args} />